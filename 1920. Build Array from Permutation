class Solution {
    public int[] buildArray(int[] nums) {
        int arr[]=new int[nums.length];
        int i=0;
        for(int n:nums){
            arr[i]=nums[nums[i]];i++;
        }
        return arr;
    }
}
